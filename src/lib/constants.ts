// Shared constants used across the app

// Projects with more clips than this threshold route through the Review screen
// before the Editor. Sessions at or below this count skip directly to the Editor.
export const REVIEW_THRESHOLD = 5;
