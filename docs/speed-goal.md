# Speed Goal

Transparency doc — not a strategy doc. Tracks progress toward the render-speed north star so we don't re-propose rejected ideas or lose sight of the real bottleneck.

## North star

- **Cold render: 5 min max** (any project size, any resolution)
- **Proxy gen: as little as possible, ~5–10 min ceiling** for a full project's background proxy batch
- Existential goal: close the gap to DaVinci Resolve's render speed — see [[feedback_no_recalibrate_render_time_target]]. Do not soften this target.

## Current speed logs (real numbers, from `%TEMP%\rushcut\render-timing-log.jsonl`)

**Big case — 19 clips, 4K, crossfade, AMF (Stagecoach test project):**
| stage | range | % of total |
|---|---|---|
| `t_total_s` | 500–705s (8.3–11.75 min) | 100% |
| `t_render_s` | 317–486s | 63–69% |
| `t_trim_s` | 94–139s | 15–25% |
| `t_normalise_s` | 22–25s | ~4% |
| `t_music_s` | 19–21s | ~3% |
| `t_zoom_s` | 3–4s | <1% |

**Smaller case — 8 clips, 4K, no transition/zoom, proxies warm:** `t_total_s` 173–215s, `t_render_s` 123–157s, `t_trim_s` 25–32s.

**Gap to north star:** big-case cold total is 1.7–2.35x over the 5-min target. `t_render_s` + `t_trim_s` = 85%+ of the gap. Everything else is noise — don't optimise it first.

**Variance diagnosed (2026-07-11):** `t_render_s` swings 317→486s (~50%) across renders of the *identical* clip set/settings. Checked against proxy contention (never fired) and WSL memory (flat 11340–11354MB across all 4 renders, no correlation with render time) — both ruled out. Diagnosis: Windows/WSL scheduler noise from other load during a back-to-back A/B testing session, not a pipeline inefficiency — unfalsifiable from pipeline logs alone. **317s is the clean-machine floor for this baseline going forward, not 486s.** `mem_avail_mb` is now logged at U1g batch start (not just completion) so a future variance event has richer data if worth revisiting.

## Outstanding hypotheses / relevant open issues (ranked)

1. **#131 (P2-Medium, RICE 35)** — GPU-accelerate or restructure the per-frame `scale=eval=frame,crop=...` Ken Burns filter in `zoom.py`. The first hypothesis in this whole investigation series aimed at the actual dominant cost bucket (`t_render_s`, 63-69% of total) rather than a peripheral lever (encoder choice, trim batching, boundary re-encode, decode hwaccel — all tried, all flat/NO-GO). Root cause identified by #113. **A NO-GO here makes it 6/6 lever-level speed investigations without a real win since #65 — that result should trigger a swimlane-pause conversation with the user, not another lever attempt.**
2. **#115 (P3-Low)** — opt-out toggle to skip the open/close boundary re-encode, follow-up to #88 (closed). Default stays ON (Best Quality) — opt-out only.
3. **#106 (P3-Low)** — copy-stream normalise for already-compliant clips. Narrow: never fires on the primary DJI HEVC workload.

`t_trim_s` has no remaining candidate after #104 closed NO-GO (below) — #99's parallelism win (~1.2x) is the ceiling for that stage barring a fundamentally different approach. Remaining north-star gap is `t_render_s` (63–69% of total) — #131 above is the first hypothesis to target it directly. **#110 (hevc_amf) and #113 (GPU decode) both shipped/probed but are NOT `t_render_s` levers** — see below; #110 is a file-size win only, #113 confirmed the real bottleneck is the CPU-bound Ken Burns filter, not decode or encode.

## Learning log (brief — tried, success/fail)

- **#116 unsharp mask (measured, REJECTED, 2026-07-14)** — `unsharp=5:5:1.0:5:5:0.0` appended post-crop in `_kenburns_vf()` (Ken Burns branch only), tested on the identical 2-clip 4K project/manifest as the lanczos test below (`[cache] MISS` confirmed, no AMF): `render_s` 181.5s -> 227.1s (+25%), past the pre-committed 200s/10% ceiling. Killed on speed alone, per the pre-committed rule — no TV-check was performed. Reverted same session, `pipeline/zoom.py` diff empty. Closes out #116's last untested filter-layer hypothesis — see `docs/quality-goal.md` "#116 verdict" for the full closure writeup.
- **#116 lanczos scale filter (measured, REJECTED, 2026-07-14)** — `flags=lanczos` added to zoom.py's scale filters, tested via a real 2-clip 4K render (no AMF, direct pipeline invocation): `render_s` 181.5s -> 259.8s (+43%), `total_s` 214s -> 294s (+37%). No visible quality benefit found (see quality-goal.md), so reverted regardless — but flagging here too since it corrects a prior assumption that zoom-graph filter tweaks are cheap (`t_zoom_s` <1% of total, per #66/#67/#79). That assumption only holds for the graph *shape*; a filter change that alters the main filter_complex encode cost (like an interpolation-algorithm flag) shows up in `t_render_s` instead, which is 63-69% of total on the big case — not free to iterate on.
- **#88 (measured, CLOSED GO, 2026-07-11 follow-up session)** — real data finally read after #114 unblocked the path. Two clean renders of Stagecoach 2025 (19 clips, 4K, both boundaries, h264_amf): `t_boundary_reencode_s` 47.4s/362.0s (13.1%) and 49.2s/368.0s (13.4%) — consistent, real, repeatable cost, not noise. Both renders `drift=0`, no fallback. Cleared the ~30-40s/>10% go/no-go threshold set going in → filed **#115** (opt-out toggle, default stays Best Quality) as the follow-up. #88 closed.
- **#88 (attempted, BLOCKED, 2026-07-11 earlier session)** — shipped timing instrumentation (`t_boundary_reencode_s`, `opening_transition_on`, `closing_transition_on`) to isolate boundary-reencode cost. Real verification render hit a new bug instead (#114): both open+close active trips a 2-frame drift guardrail → monolithic fallback → AMF OOM → libx264 OOM-killed WSL. No `t_render_s`/`t_boundary_reencode_s` data obtained. Both boundary re-encodes themselves succeeded before the drift check killed the run — no evidence yet that boundary-reencode cost itself is the problem.
- **#114 (fixed, 2026-07-11, same-day follow-up)** — root-caused via per-boundary frame instrumentation: the open boundary's `offset=0` xfade contributes 0 frames (ffmpeg xfade semantics: `output_duration = offset + second_stream_duration`), not the assumed +3; close boundary measured +4, not +3. Fixed by deriving the drift guardrail from real measured per-boundary deltas (`oc_actual_frame_delta`) instead of a theoretical duration formula, plus a `_BoundaryDriftError` safety net so a correctness-check failure on this path can never again cascade into the monolithic/OOM/WSL-crash sequence regardless of root cause. Verified live: Stagecoach 2025 (20 clips, 4K, both transitions) now completes with `drift=0`, `segmented render complete (no fallback)`. #88 unblocked, not yet re-attempted.
- **#65 (closed, SUCCESS)** — AMF quality preset raised + eliminated full-film double-encode for open/close (post-pass now only re-encodes the boundary segment, not the whole film). Real win.
- **#84 (closed, REJECTED)** — AMF `-quality "balanced"` tested as an A/B against `"quality"`. Zero speed gain at current 40M/50M bitrate, visible quality loss. Reverted. **Don't re-propose without a bitrate change too.**
- **#99 (closed, PARTIAL)** — Parallelised Step 2 trim. Only 1.1–1.3x, not the expected 3–4x — real ceiling is a ~7–8s fixed cost per FFmpeg invocation, not a parallelism problem. See #104 for the actual fix direction.
- **#96 (closed, MIXED)** — Fixed trim precision (keyframe-snap bug adding ~1s of wrong footage). Correctness win, but changed Step 2 from stream-copy to re-encode — added the cost #99/#104 are now dealing with.
- **#98 (closed, FIX)** — U1g tail-batch crash fix; was silently falling back to slow monolithic render on the last batch.
- **#100 (closed, DIAGNOSIS)** — `t_normalise_s=25s` on a full proxy-skip render confirmed to be B-0 pre-trim time bucketed under the "normalise" timer label, not a regression. Logs-only, no code change. Floor magnitude (does B-0's stream-copy hit a fixed per-invocation floor like #104's re-encode?) is still open, cross-referenced on #104.
- **2026-07-11 diagnosis session — 5 proposed speed ideas, 4 rejected:**
  - Pre-bake xfade segments at proxy time — high complexity, mostly redundant with the V4.1 render cache, and repeats a pattern (pre-bake-and-cache) already tried and reverted for zoom (#67/#79).
  - Parallel U1g batch encoding — **reintroduces OOM.** U1g's `BATCH_SIZE=4` sequential design exists specifically because one 4K batch peaks at 6–9.7 GB against a 12 GB WSL budget; running batches in parallel would blow that budget. Also AMF hardware contention makes concurrent encodes *slower*, not faster (documented precedent in the proxy-batch concurrency guard).
  - Pre-compute/cache zoom VF strings — near-zero payoff (`t_zoom_s` is <1% of total); `build_zoom_vf` is a cheap string-builder, not a heavy computation.
  - Second-tier music/loudnorm cache — **already built** (V4.1 render cache excludes music fields from the signature and reapplies them as a cheap remux on every hit).
  - **Kept:** copy-stream normalise for already-compliant clips (no compliance check exists in `normalise.py` today) — real but narrow, since primary DJI HEVC footage is never "already compliant" H.264. Filed as **#106** (P3-Low).
- **2026-07-11 variance diagnostic pass — DIAGNOSED, not a pipeline problem.** Correlated the 317–486s `t_render_s` spread against proxy-contention logs and `mem_avail_mb` (added per-batch logging as part of this pass) across the 4 matching job logs (matched by timestamp proximity — `render-timing-log.jsonl` has no `job_id` field, see LEARNINGS.md). Contention warning never fired; memory was flat (11340–11354MB) regardless of render speed. Conclusion: external machine load during A/B testing, not pipeline inefficiency. **317s recorded as the clean-machine floor.**
- **#104 (scoping measurement, 2026-07-11) — GO, floor is encode-specific.** Measured whether a pure `-c copy` invocation pays #99's ~7-8s floor. Serial test: reencode 2.63s avg vs. copy 0.26s avg (10x). Reproduced #99's exact production conditions (4-way `ThreadPoolExecutor`, 4 threads/worker): parallel reencode 7.88-7.90s/job (matches #99 exactly), parallel copy 0.28-0.29s/job (no floor at all). Conclusion: the floor is libx264 encode work under thread-capped contention, not generic FFmpeg process-spawn overhead — partial-GOP trim's extra invocations would be cheap if they're copy/concat, not re-encode. Also found (after fixing a CSV-column-order bug in the probe script) a real periodic ~8.34s GOP in a real normalised DJI clip (no scene cuts, so x264 falls back to default max keyint=250) — a realistic 12s trim window has ~55% sitting after an internal keyframe, i.e. real copyable middle, not a hypothetical one. Full data: issue #104 comment. No code shipped — batching should be evaluated first (same win, lower risk).
- **#107 (scoping measurement, 2026-07-11) — NO-GO, closed.** Measured whether batching all of a job's Step 2 trims into ONE ffmpeg process (N inputs, N independent filter chains, N output files — not a literal concat, which would have broken zoom/volume/transitions' per-clip file contract) amortizes the ~7-8s/job encode floor. Result: the opposite — batched avg 11.82s (default `rc-lookahead`) / 10.90s (`rc-lookahead=0`) vs. current per-clip avg 8.83s, on 6 real DJI clips under the same `MAX_PARALLEL_TRIM=4` contention shape. Batching is ~25-35% **slower**, not faster. Memory: batching roughly doubled peak WSL memory vs. current (~2.75GB vs ~1.4GB above baseline for N=6, no throttle analogous to `MAX_PARALLEL_TRIM` in the one-process design) — projects to ~8-9GB for a 19-clip project, real OOM risk. `rc-lookahead` turned out not to be the dominant memory driver at this scale (estimated ~712MB contribution from lookahead buffers; measured difference was ~0MB) — don't re-propose that specific lever without new evidence. Full data + GO/NO-GO writeup: issue #107 comment.
- **#85 (investigation, 2026-07-11) — GO, closed. Follow-up implementation filed as #110.** Standalone ffmpeg benchmark (not the live pipeline — scope was investigation-only): `hevc_amf` at the same 40M bitrate as current `h264_amf` encoded ~2.3x faster (7.1s vs 16.5s, 12s/4K test clip); at half bitrate (20M, the expected equivalent-quality target), still ~2.1x faster at ~50% file size. Visual check across all 3 tiers showed no artifacts. Playback gate passed via a scripted Media Foundation decode+play test (real position-advance confirmed, computer-use GUI access was denied this session) — the original `LEARNINGS.md:327` "WMP rejects HEVC" blocker turned out to be about the *absence of a decoder*, not WMP itself; this machine has the MS HEVC Video Extension installed and playback works fine. **Real residual risk reframed for #110:** the HEVC Video Extension isn't guaranteed on every end-user machine (separate MS Store package, not bundled by default) — that's the actual thing to scope before shipping, not WMP compatibility. No production code changed this session.
- **#110 (implementation + real measurement, 2026-07-11) — SHIPPED as a file-size win, NOT a speed win.** hevc_amf opt-in (`RUSHCUT_USE_HEVC_AMF`) implemented and A/B tested on Stagecoach 2025 (19 clips, 4K, crossfade, warm proxies). Real `t_render_s`: 333.0s (h264_amf) vs 319.0s (hevc_amf) — only ~4% faster, not #85's standalone ~2.1-2.3x. File size: 340.2 MB vs 675.7 MB — ~50%, exactly as #85 predicted. Zero AMF fallbacks, zero visual corruption on pan-heavy footage TV-check. Same root-cause pattern as #104: `t_render_s` in the real U1g pipeline is dominated by multi-input decode + filter_complex xfade compositing, not the final encode call — hevc_amf only speeds up the latter. Not a `t_render_s` lever without also addressing #101/#88-territory compositing cost. Full writeup: LEARNINGS.md "FFmpeg — hevc_amf's encode-side speed win does not survive the real U1g pipeline".
- **#101 (logs-only measurement, 2026-07-11) — GO (keep as-is), closed.** Measured both open questions from real data instead of proposing a resolution/tiering change. Found the issue's own premise about `-hwaccel d3d11va` (#92) was stale in the wrong direction: #92 only touches `src-tauri/src/lib.rs`'s proxy-gen path (Windows-side ffmpeg.exe), never `pipeline/normalise.py` (WSL-side, used at render time) — full-source-normalise cost is unchanged by #92. No large-scale (19-clip) cold-normalise data point exists in `render-timing-log.jsonl` (every 19-clip render on record is proxy-warm); extrapolated from small-scale data (4 clips, 1080p output, 4K source: 25-29s → ~6.5-7.5s/clip) to ~125-140s for 19 clips — explicitly not measured at scale, decided not worth a disruptive real cold-render test to verify further. Weighed against real 2160p proxy-gen cost from `proxy-bg.log` (~100-150s/clip, one-time, background/non-blocking): amortizes after a single avoided cold render, and real usage (`jobs` table) shows the same project re-rendered warm multiple times per sitting. **Conclusion: 2160p proxy strategy holds as-is, no code change.** Caveat: re-render-frequency evidence is dev-testing-only (1 user, heavy A/B sessions), not organic end-user data — revisit only if real end-user telemetry ever shows a render-once-then-abandon pattern. Full writeup: issue #101 (closed) comment.
- **#124 (implementation + real measurement, 2026-07-14) — SHIPPED then REVERTED same day. NO-GO.** `decide_clip_source()` briefly bypassed proxy reuse (forced full normalise) for any clip with `zoom_mode` set, per #118's TV-check finding. Real A/B (not extrapolated): a 2-clip 4K project with both clips zoomed vs. the same clips with `zoom_mode` forced to `"none"` — zero-zoom control: `2 proxy-skip/0 normalise`, `normalise_s=5s` (unchanged baseline); all-zoom: `0 proxy-skip/2 normalise`, `normalise_s=98s`. **Real cost: +93s/2 zoomed clips (~46.5s/clip) at 4K** — extrapolates to roughly +880s on a Ken-Burns-heavy 19-clip project, on top of a big-case baseline already 1.7-2.35x over the 5-min ceiling. A follow-up TV check on the real shipped code path (not #118's debug `force_normalise` flag) found **no visible quality improvement** — the cost bought nothing. Reverted the same day. **Lesson: speed is priority #1 — do not default-enable a change with a real, non-trivial measured speed cost until the quality benefit is confirmed via the actual shipped code path (not a debug-flag proxy for it), and quantified at realistic project scale, not just a 2-clip sample.** See `docs/quality-goal.md` Hypothesis #1 and `feedback_confirm_benefit_before_speed_cost.md`.
- **#113 (probe + real measurement, 2026-07-15) — NO-GO, closed.** Standalone probe on real Windows-native `ffmpeg.exe` (the AMF interop-bridge binary): `-hwaccel d3d11va` vs software decode, real captured filter_complex (Ken Burns zoom + concat, 2x12s 4K clips), h264_amf CQP encode, 3 runs/variant. Result: hwaccel d3d11va **~9.2% SLOWER** (median 50.57s vs 46.30s), ranges non-overlapping — not noise, wrong direction. Both variants correct (720 frames, no corruption). Root cause: `scale=eval=frame,crop=...` Ken Burns filter is CPU-bound and dominates render time for these clips, not decode — GPU decode just adds dispatch/sync overhead on top. **Don't re-propose plain hwaccel decode without new evidence; if revisited, test against a non-zoomed clip batch first** (Ken Burns may not be the bottleneck there). Full data: issue #113 (closed) comment.
- **#104 (implementation + real measurement, 2026-07-11) — NO-GO, closed.** Built `trim_smart()` (partial-GOP trim) after the scoping pass's GO: re-encode only the head segment up to the first internal keyframe, `-c copy` the tail straight through to `end_s` (corrected mid-session from an original 2-keyframe/3-segment design — only the leading edge needs keyframe alignment, `-c copy` with `-t` on the end just truncates, no #96-style snap). All 3 pre-implementation gates passed: IDR confirmed via `trace_headers` on both libx264 and h264_amf output; concat-only cost ~0.4s/job (well under the 1s bar); same-GOP fallback verified on a real 7.27s clip. A follow-up isolation measurement even confirmed the core theory — a shortened head-reencode really is cheaper under contention (4.16s vs 5.52s avg, real 1.36s/job saving). **But the real end-to-end pipeline run (old `trim()` vs `trim_smart()`, same clips/windows/contention) was 19-31% SLOWER across two separate runs**, not faster — individually-cheap pieces (probe + copy + concat) didn't compose into a net win. Frame-accuracy held throughout (not a correctness bug). Reverted `render.py`'s wiring back to plain `trim()`; `trim_smart()` stays in `pipeline/trim.py` unused, commented with the regression numbers. Full writeup: issue #104 (closed) + LEARNINGS.md "Pipeline — partial-GOP trim" entry. **`t_trim_s` has no further speed candidate after this** — #99's ~1.2x parallelism win is the ceiling for that stage barring a fundamentally different approach (not batching, not partial-GOP).
